@model IEnumerable<CompareMotos.Models.Transmission>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeTransmission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmountGear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeTransmission.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmountGear.AmountGearId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransmissionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransmissionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransmissionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
